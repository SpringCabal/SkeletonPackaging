Contains base folders (common for both Linux and Windows architectures) as well as some scripts to automate packaging.
Should probably be rewritten in Python or some other portable language later on...
